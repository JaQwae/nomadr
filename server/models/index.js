// Package all the models
const Country = require('./country');
const Income = require('./income');
const User = require('./User');

// Export models
module.exports = { Country, Income, User };
